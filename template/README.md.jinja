{#- Rendered once and never updated, like pyproject.toml, because a repository's
    README is its own. It exists in the template for two reasons: SUPPORT.md
    links to it for an overview, which was a broken link in every fresh render,
    and repo-review's PY002 asks for one. -#}
# {{ github_repository.split('/')[1] }}

Replace this file.

## Checks

```sh
pre-commit install --install-hooks
nox            # lint, tests, typing
nox -s review  # this repository against the Scientific Python Development Guide
```
{%- if has_python %}

## Measurements

The table is `pyperf compare_to` over `benchmarks/baselines.json` and
`benchmarks/solutions.json`, which [WORKFLOW.md](WORKFLOW.md) describes; the
mdsh hook runs the line below at each commit and rewrites what follows it.

<!-- prettier-ignore-start -->
`> uv run python -m pyperf compare_to --table --table-format md benchmarks/baselines.json benchmarks/solutions.json`
<!-- prettier-ignore-end -->
{%- endif %}
