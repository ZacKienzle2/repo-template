[#- Rendered once and never updated, because a repository's README is its
own. It exists in the template because SUPPORT.md links to it for an
overview, which was a broken link in every fresh render. -#]

# (( github_repository.split('/')[1] ))

Replace this file.

## Checks

```sh
prek install
prek run --all-files
```
