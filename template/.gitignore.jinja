# Rendered by copier from github/gitignore and the gitignore_own answer.
# % if has_latex
# ~
[% filter trim %][% include "gitignore/TeX.gitignore" %][% endfilter %]
# % endif
# % if has_node
# ~
[% filter trim %][% include "gitignore/Node.gitignore" %][% endfilter %]
# % endif
# % if gitignore_own
# ~
# This repository's own, the gitignore_own answer in .copier-answers.yml.
# % for path in gitignore_own
(( path ))
# % endfor
# % endif
